@model MilesBackOffice.Web.Models.User.CreateUpgradeViewModel


<div id="newUpgrade">
    <div>
        <h2>New Premium Upgrade</h2>
        <form method="post" asp-action="CreateUpgrade">
            <div class="form-group">
                <label asp-for="Title"></label>
                <input type="text" asp-for="Title" required />
                <span asp-validation-for="Title"></span>
            </div>
            <div class="form-group">
                <label asp-for="PartnerId"></label>
                <select type="text" asp-for="PartnerId" required asp-items="Model.PartnersList"><option value="">Select a Partner</option></select>
                <span asp-validation-for="PartnerId"></span>
            </div>
            <div class="form-group">
                <label asp-for="FlightId"></label>
                <select type="text" asp-for="FlightId" required asp-items="Model.Flights"><option value="">Select a flight</option></select>
                <span asp-validation-for="FlightId"></span>
            </div>
            <div class="form-group">
                <label asp-for="Quantity"></label>
                <input type="number" asp-for="Quantity" required min="1" value="1"/>
                <span asp-validation-for="Quantity"></span>
            </div>
            <div class="form-group">
                <label asp-for="Price"></label>
                <input type="number" required min="0" asp-for="Price" />
                <span asp-validation-for="Price"></span>
            </div>
            <button type="submit" class="btn btn-outline-success">Create</button>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
